/target
**/*.rs.bk
.DS_Store
*.tqf
/models/
# The project-local retrieval index (spec §218). Generated by
# `tqf sync`, rebuildable from source, and machine-specific.
/.tqf/
/.tqf-dev/
/.freebuff/
/swift/.build/
/.mimocode/

# Local checkpoint paths for `just qual-*` (loaded by the justfile's
# `set dotenv-load`). Machine-specific and often absolute — never commit.
.env

# phase-verify's per-staging-state sentinel.
/.claude/.phase-verify-ok
